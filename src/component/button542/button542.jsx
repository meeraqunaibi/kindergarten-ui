import React from 'react'
import './button542.css'

const Button542 = (props) => {
  return (
    <button className='button542' type={props.type} >{props.content}</button>
  )
}

export default Button542